
<div class="card mb-3">
  <div class="card-body">
    <p><strong><%= comment.name %></strong> <%= time_ago_in_words(comment.created_at) %></p>
    <p class="card-title"><%= comment.content %></p>

      <p class="ml-3"> <%= link_to "Delete !", [comment.post, comment], data: {
          turbo_method: :delete,
          turbo_confirm: "Are you sure?"
        } %>
      </p>

  </div>
</div>